diff -burN InsightToolkit-4.7.2.orig/Modules/IO/Meta/src/itkMetaImageIO.cxx InsightToolkit-4.7.2/Modules/IO/Meta/src/itkMetaImageIO.cxx
--- InsightToolkit-4.7.2.orig/Modules/IO/Meta/src/itkMetaImageIO.cxx	2018-01-11 10:57:08.721822608 +0100
+++ InsightToolkit-4.7.2/Modules/IO/Meta/src/itkMetaImageIO.cxx	2018-01-11 13:54:10.746375903 +0100
@@ -73,6 +73,14 @@
     return false;
     }
 
+  if (  fname.size() >= 1024 )
+    {
+      itkExceptionMacro( << "File cannot be read: "<< fname << std::endl
+			 << "Reason: Maximum length of file name(fullpath) for metaio image is 1024"
+			 << std::endl );
+      return false;
+    }
+  
   return m_MetaImage.CanRead(filename);
 }
 
@@ -500,6 +508,14 @@
     {
     return false;
     }
+  if (  filename.size() >= 1024 )
+    {
+      itkExceptionMacro( << "File cannot be written: "<< filename << std::endl
+			 << "Reason: Maximum length of file name(fullpath) for metaio image is 1024"
+			 << std::endl );
+      return false;
+    }
+  
 
   std::string::size_type mhaPos = filename.rfind(".mha");
   if ( ( mhaPos != std::string::npos )
